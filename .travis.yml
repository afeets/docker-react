language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -f Dockerfile.dev -t andyfeetenby/docker-react .

script:
  - docker run -e CI=true andyfeetenby/docker-react npm run test
